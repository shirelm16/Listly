<?xml version="1.0" encoding="utf-8" ?>
<mopups:PopupPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:mopups="clr-namespace:Mopups.Pages;assembly=Mopups"
             x:Class="Listly.View.AddListMenuPopup"
             BackgroundColor="#40000000">

    <Border BackgroundColor="{StaticResource Surface}"
            StrokeThickness="0"
            Padding="8"
            Margin="20,80,20,0"
            VerticalOptions="Start"
            HorizontalOptions="End"
            StrokeShape="RoundRectangle 16">
        <Border.Shadow>
            <Shadow Brush="Black" Opacity="0.15" Radius="16" Offset="0,4"/>
        </Border.Shadow>

        <StackLayout Spacing="0">
            <!-- New List -->
            <Border StrokeThickness="0"
                    BackgroundColor="Transparent"
                    Padding="16,14">
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding NewListCommand}"/>
                </Border.GestureRecognizers>
                <HorizontalStackLayout Spacing="12">
                    <Label Text="📋"
                           FontSize="18"
                           VerticalOptions="Center"/>
                    <Label Text="New List"
                           FontSize="16"
                           TextColor="{StaticResource OnSurface}"
                           VerticalOptions="Center"/>
                </HorizontalStackLayout>
            </Border>

            <!-- Divider -->
            <BoxView HeightRequest="1"
                     BackgroundColor="{StaticResource Outline}"
                     Margin="16,0"
                     Opacity="0.4"/>

            <!-- From Recipe -->
            <Border StrokeThickness="0"
                    BackgroundColor="Transparent"
                    Padding="16,14">
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding FromRecipeCommand}"/>
                </Border.GestureRecognizers>
                <HorizontalStackLayout Spacing="12">
                    <Image Grid.Column="0"
                           HeightRequest="20"
                           WidthRequest="20"
                           Source="chef_hat.png"
                           VerticalOptions="Center"/>
                    <Label Text="Import from Recipe"
                           FontSize="16"
                           TextColor="{StaticResource OnSurface}"
                           VerticalOptions="Center"/>
                </HorizontalStackLayout>
            </Border>
        </StackLayout>
    </Border>
</mopups:PopupPage>